// Novo appsettings criado para Staging (apenas colocar appsetting. no nome que já identifica)

{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}